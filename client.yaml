schema_version: 1
kind: client

client:
  id: ""                 # kebab-case, matches the repo name client-{id}
  name: ""               # public-facing name
  legal_name: ""
  status: prospect       # prospect | active | paused | former
  timezone: ""
  approver: ""           # human who approves public content and launches

engagement:
  services: []           # e.g. [website-launch]
  phase: assess          # assess | design | build | validate | transfer | improve
  notes: ""

repositories: {}         # e.g. website: client-{id}-website

# Anything too large, raw, or sensitive for Git: one entry per item.
external_storage: []
# - name: product-photo-archive
#   location: ""         # bucket, drive, or system name — never a credential
#   contents: ""
#   access: ""

# Secret NAMES only. Values live in the deploy platform or secret manager.
secrets: []
# - name: FORM_WEBHOOK_URL
#   used_by: website contact form
#   stored_in: deploy platform environment

data_notes: []           # client-specific data restrictions, if any
