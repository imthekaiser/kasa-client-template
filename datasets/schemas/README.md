# Dataset Schemas

Store field definitions, mappings, validation rules, and other non-sensitive dataset documentation here.

Sanitized samples may be stored only when they contain no restricted information and are registered in `datasets/catalog.yaml`. Raw datasets and production exports must remain in approved external storage.
