# Intake Source Documents

Store normal confidential intake documents here only when Git is an approved canonical location for the file.

Do not store secret values, restricted data, production exports, database backups, large archives, or files requiring guaranteed deletion. Register externally stored source documents in `references/external-sources.yaml` and `client.yaml`.
